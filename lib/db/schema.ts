// Drizzle schema lives here. Phase 2 builds out the full schema per ARCHITECTURE.md §5
// (agents, contacts, interactions, listings, showings, transactions, etc.).
// Empty for Phase 1 so drizzle-kit has a target file.

export {};
